-r requirements.txt

# SKY130 1.0.0 and ihp-gdsfactory 2.0.0 require incompatible GDSFactory
# minor releases. Keep this dependency set in a dedicated SKY130 environment.
gdsfactory==9.40.1
kfactory==2.4.7
doroutes==0.5.0
python-dotenv>=1.2.1

# The published sky130 wheel declares gdsfactoryplus even though the primitive
# PCells used by ShapeIC do not import it. Its current dependency metadata is
# incompatible with kfactory 2.4.7, so install the PDK package separately:
#
#   python -m pip install --no-deps sky130==1.0.0
